package processorsimulation;
/**
 * Not all values of the ProcessState enum are currently used in the program,
 * but this allows for expansion of the project.
 * 
 * @author Malky
 *
 */
public enum ProcessState {
	READY, BLOCKED, SUSPENDED_READY, SUSPENDED_BLOCKED, FINISHED
}
